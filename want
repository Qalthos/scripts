#!/bin/bash

case $(distro_check.sh) in
"Arch")
    yaourt -S $1
    ;;
"Debian" | "Ubuntu")
    sudo aptitude install $1
    ;;
"Fedora")
    sudo yum install $1
    ;;
*)
    echo "Sorry, I don't know that one."
    ;;
esac
